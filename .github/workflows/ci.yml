name: my second pipeline

on: push

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Check Out Code
              uses: actions/checkout@v4

            - name: Build the Image
              run: docker build -t ceejayme/dev-app-profiles .
            
            - name: list the Image
              run: docker images